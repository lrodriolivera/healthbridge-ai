/// {{Description}}
/// Generated by HealthBridge AI
Class {{Namespace}}.Production Extends Ens.Production
{

XData ProductionDefinition
{
<Production Name="{{Namespace}}.Production" TestingEnabled="true" LogGeneralTraceEvents="true">
  <Description>{{Description}}</Description>
  <ActorPoolSize>2</ActorPoolSize>

  {{#each Services}}
  <Item Name="{{Name}}" Category="{{Category}}" ClassName="{{ClassName}}" PoolSize="{{PoolSize}}" Enabled="{{Enabled}}" Foreground="false" InactivityTimeout="0" Comment="{{Comment}}">
    {{#each Settings}}
    <Setting Target="{{Target}}" Name="{{Name}}">{{Value}}</Setting>
    {{/each}}
  </Item>
  {{/each}}

  {{#each Processes}}
  <Item Name="{{Name}}" Category="{{Category}}" ClassName="{{ClassName}}" PoolSize="{{PoolSize}}" Enabled="{{Enabled}}" Foreground="false" InactivityTimeout="0" Comment="{{Comment}}">
    {{#each Settings}}
    <Setting Target="{{Target}}" Name="{{Name}}">{{Value}}</Setting>
    {{/each}}
  </Item>
  {{/each}}

  {{#each Operations}}
  <Item Name="{{Name}}" Category="{{Category}}" ClassName="{{ClassName}}" PoolSize="{{PoolSize}}" Enabled="{{Enabled}}" Foreground="false" InactivityTimeout="0" Comment="{{Comment}}">
    {{#each Settings}}
    <Setting Target="{{Target}}" Name="{{Name}}">{{Value}}</Setting>
    {{/each}}
  </Item>
  {{/each}}

</Production>
}

}
